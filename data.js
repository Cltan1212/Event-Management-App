/**
 * An array containing category objects.
 * @type {Array.<Category>}
 */
const categoryDb = [];

/**
 * An array containing event objects.
 * @type {Array.<Event>}
 */
const events = []

module.exports = {
    categoryDb,
    events
};
